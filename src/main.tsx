import React from 'react';
import ReactDOM from 'react-dom/client';
import { Flynanz } from './Flynanz.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Flynanz />
  </React.StrictMode>,
);
